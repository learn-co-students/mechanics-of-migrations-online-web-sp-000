require_relative './config/environment'
require 'sinatra/activerecord/rake'
#Tasks appearing in (bundle exec) rake -T are stored in sinatra/activerecord/rake

task :console do
  require 'irb'
  ARGV.clear
  IRB.start
end
